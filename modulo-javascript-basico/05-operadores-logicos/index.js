/*
    ==  operador: igual ---  a==b --- verdadeiro se a for igual a b
    ===  operador: identico --- a===b --- verdadeiro se a for identico a b
    != operador: diferente --- a != b --- verdadeiro se o a for diferente de b
    !== operador: não identico --- a !== b --- verdadeiro se o a não for identico a b
    < operador: menor que --- a < b --- verdadeiro quando a for menor que b
    <= operador: menor igual que --- a <= b --- verdadeiro  quando a for menor ou igual a b
    > operador: maior que --- a > b --- verdadeiro quando a for maior que b
    >= operador: maior igual que --- a >= b --- verdadeiro  quando a for maior ou igual a b
*/

const a = 3
const b = 3

console.log(a==b)